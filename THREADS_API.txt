Learn Piano Threads API setup
==============================

Project:
- Product: Learn Piano / Piano Codex Coach
- Threads account: @learn_piano_games
- Intended profile visibility: public

Meta application:
- App name: Personal Codex Threads
- Threads App ID: 1388176256575019
- Enabled permissions: threads_basic, threads_content_publish

Multi-account model:
- Each Threads account uses its own named local profile and its own user token.
- The Learn Piano profile name is `learn-piano`.
- Adding another brand or account must create another named profile; tokens and
  account identity state must never be shared between profiles.

Local runtime:
- Client: /Users/phil/Documents/Codex Projects/Personal Projects Codex/scripts/threads_api.py
- Learn Piano project binding:
  /Users/phil/Documents/Codex Projects/learn-piano-play-songs
- Private credentials are stored outside this repository in the protected
  Personal Codex runtime directory. App secrets and access tokens must never be
  copied into this file, the Eponymical Games repository, logs or chat.

Publishing safeguards:
- The token is accepted only after the API confirms it belongs to
  @learn_piano_games.
- Before every publishing operation, the client checks both the configured
  username and the persisted Threads user id.
- Creating a media container does not publish it.
- A post can be published only with an explicit publication confirmation and
  an exact match to the separately approved text.
- Setup does not include a test post or any other public post.

Current status as of 2026-07-24:
- The public Threads profile @learn_piano_games has completed onboarding.
- The account accepted the tester invitation for Personal Codex Threads.
- Meta authorization was completed with only `threads_basic` and
  `threads_content_publish`.
- A replacement long-lived token was installed in the private `learn-piano`
  runtime profile after `/me` verified username `learn_piano_games` and Threads
  user id `27770957212542691`.
- The token is scheduled to expire on 2026-09-22 at 15:19:45 UTC. The client
  begins refresh handling within seven days of expiry.
- The first generated credential was revoked and its application authorization
  removed after internal browser tooling exposed it during setup. It was not
  retained or used for publishing.
- Temporary token-import files were deleted after the verified replacement was
  stored with file mode 0600.
- No public post has been created or published.
